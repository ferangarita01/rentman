# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Rentman Backend - Environment Variables Template
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 
# ⚠️  PRODUCTION WARNING:
# DO NOT use .env files in production. Use Google Cloud Secret Manager.
# This file is for LOCAL DEVELOPMENT ONLY.
#
# Setup Instructions:
# 1. Copy this file to .env
# 2. Fill in the actual values (get them from your team lead)
# 3. Set USE_LOCAL_SECRETS=true for development
# 4. NEVER commit .env to Git
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Server Configuration
PORT=8080

# Google Cloud Project
GCP_PROJECT_ID=agent-gen-1

# Secret Management Mode
# Set to 'true' for local development to use .env file
# Set to 'false' or omit for production to use Secret Manager
USE_LOCAL_SECRETS=true

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Secrets (Only for local development)
# In production, these are automatically loaded from Google Cloud Secret Manager
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Stripe API Keys (Get from https://dashboard.stripe.com/apikeys)
# Example: sk_test_51ABC...
STRIPE_SECRET_KEY=

# Supabase Configuration (Get from https://app.supabase.com/project/*/settings/api)
SUPABASE_URL=https://uoekolfgbbmvhzsfkjef.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Webhook Security
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
WEBHOOK_SECRET=

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Production Deployment with Secret Manager
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 
# Manage secrets using the provided utility:
#
#   .\manage-secrets.ps1 list                        # List all secrets
#   .\manage-secrets.ps1 get STRIPE_SECRET_KEY       # View a secret
#   .\manage-secrets.ps1 update WEBHOOK_SECRET "val" # Update a secret
#   .\manage-secrets.ps1 sync                        # Sync from backup
#
# Secrets are automatically loaded in Cloud Run when USE_LOCAL_SECRETS is false
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
